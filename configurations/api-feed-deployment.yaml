apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-feed
  labels:
    app: api-feed
specs:
  replicas: 1
  selector:
    matchlabels:
      app: api-feed
  template:
    metadata:
      labels:
        app: api-feed
    spec:
      containers:
        name: api-feed
        image: moustafaashraf/udagram-api-feed
        imagePullPolicy: Always
        resources:
          requests:
            memory: "128Mi"
            cpu: "500m"
          limits:
            memory: "1024Mi"
            cpu: "500m"
        envForm:
          secretRef:
            name: env-secret
          configMapRef:
            name: env-config
          secretRef:
            name: aws-secret
        ports:
          containerPort: 8080
