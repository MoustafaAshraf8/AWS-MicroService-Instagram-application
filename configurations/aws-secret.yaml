# Create aws-secret.yaml file to store your AWS login credentials.
# Replace ___INSERT_AWS_CREDENTIALS_FILE__BASE64____ with the Base64 encoded credentials
# (not the regular username/password).
apiVersion: v1
kind: ConfigMap
metadata:
  name: env-config
data:
  POSTGRES_HOST: postgres.c5wpoymggfyb.us-east-1.rds.amazonaws.com
  POSTGRES_DB: postgres
  AWS_BUCKET: rn:aws:s3:::bucket-2110853-0
  AWS_REGION: us-east-2
  JWT_SECRET: testing
  AWS_PROFILE: default
  URL: http://localhost:8100
